# Day 3 – HuggingFace Transformers Dependencies
# Install with: pip install -r requirements.txt

# Core HuggingFace libraries
transformers>=4.40.0
tokenizers>=0.19.0
datasets>=2.19.0

# PyTorch (CPU build — sufficient for GPT-2 and DistilBERT)
# For GPU builds, visit: https://pytorch.org/get-started/locally/
torch>=2.2.0

# Acceleration utilities (required for device_map="auto" and large models)
accelerate>=0.29.0

# Sentence pieces tokenizer (used by T5, LLaMA, etc.)
sentencepiece>=0.2.0

# Optional: 4-bit / 8-bit quantization for large models (Linux/GPU only)
# bitsandbytes>=0.43.0
